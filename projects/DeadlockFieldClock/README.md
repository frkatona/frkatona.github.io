# Deadlock Field Clock

A static, GitHub Pages-compatible Deadlock timing dashboard. Open `index.html` directly or serve the repository root with any static web server.

The project has no build step or external runtime dependencies. Match events and timer behavior live in `app.js`; presentation lives in `styles.css`; game UI assets are stored locally in `icons/`.
